# 変更履歴

版ごとの変更をここに書く。**ストアの「変更点」欄にそのまま貼れる粒度**で、
利用者から見て何が変わったかを書く。内部の整理はコミット履歴に任せる。

日付は JST。

## 版の上げ方

```bash
npm version patch   # 0.1.0 -> 0.1.1  不具合の修正だけ
npm version minor   # 0.1.0 -> 0.2.0  機能を足した
npm version major   # 0.1.0 -> 1.0.0  使い方が変わる / 最初の公開
```

`package.json` が更新され、コミットと `v0.1.1` のようなタグが同時に作られる。
**`manifest.config.ts` は `package.json` を読むので、直す場所はここ 1 つ。**

上げる前に、この文書の「未リリース」を `## 0.1.1 - 2026-09-20` のような見出しへ
書き換え、新しい「未リリース」を空で足す。

---

## 未リリース

**エディットモードにテロップを足した。** 区間の一覧の下の「＋ テロップ」で作り、
文言と出す時間を決めると、録画に焼き込まれる。文字サイズ・フォント・文字の色・
縁取りの色・縁取りの太さは ⚙ で決める。プレイヤーの上で録画と同じ見た目を
確かめられる。

**区間・テロップ・設定を画面右側のパネルに移した。** 区間やテロップが増えても、プレイヤー直下の
IN / OUT・録画と拡大バーはページをスクロールせずに操作できる。パネルは見出しで畳める。

## 1.1.0 - 2026-09-23

**エディットモードを足した。** ⚙ の「モード」で切り替える。

- 同じ動画の中の複数箇所を拾って、1 本のクリップに繋げられるようになった
- 「＋ 区間を追加」で区間を増やし、一覧から選んで IN / OUT や拡大バーで調整する
- 区間は**拾った順**に繋がる。後ろの場面を先に置ける
- 重なる区間もそのまま残るので、同じ場面を 2 回入れられる
- 最大秒数は**合計**で見る。区間が増えるほど録画にかかる実時間も伸びる

シンプルモード (既定) の使い勝手は変えていない。

## 1.0.0 - 2026-09-12

最初の版。

### できること

- YouTube の再生画面で **IN / OUT** を指定し、その区間を録画する
- プレイヤー直下の**拡大バー**でハンドルをドラッグして範囲を調整する。
  トラックを押すとその位置から再生でき、「範囲を見る」で通しの確認もできる
- 録画中に **中止** できる。範囲は残るのでそのまま録り直せる
- 録画したクリップを **X の投稿画面へ本文つきで自動添付**する。
  **投稿ボタンは押さない。** 最終確認は必ず手元に残る
- 投稿した後も範囲とクリップが残り、**録り直さずにもう一度投稿**できる
- **⚙ から設定**できる
  - ハッシュタグ (**チャンネルごと**に持つ)
  - クリップの最大秒数 (既定 60 秒、1〜140 秒)
- ライトとダークの両テーマに追従する

### できないこと

- 録画は**実時間かかる**。30 秒のクリップには 30 秒かかる
- **画質は録画時に再生している画質になる**。720p 未満で始めると警告を出す
- DRM 保護された動画は録画できない
- 録画中に広告が挟まると中断する
- MP4 で録画できない環境 (プロプライエタリコーデック無しの Linux Chromium など)
  では投稿まで進めない
- **動画ファイルをディスクに書き出さない。** クリップはブラウザの中に留まる
